master {
    new_status = master
	orientation = center
	slave_count_for_center_master = 0
	mfact = 1.0
	always_keep_position = true
}

bind = ALT_L, Tab, cyclenext
bind = ALT_L, Tab, swapnext

bind = ALT_L&Shift_L, Tab, cyclenext, prev
bind = ALT_L&Shift_L, Tab, swapnext, prev
